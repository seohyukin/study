<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sist.app.mapper.BbsMapper">
  
    <!-- 아래 list가 호출하는 복합결과 객체 -> 여기 올 때마다 BbsVO를 만들고 이들을 list로 묶어서 보내는 역할 -->
    <resultMap id="map1" type="com.sist.app.vo.BbsVO">
        <!-- property: 멤버변수 -->
        <id property="b_idx" column="b_idx"/>
        <!-- property: 멤버변수 | ofType: 수집물 종류 | select: ofType을 주는 곳(위치가 같으면 경로 생략하고 commList만 쓰기) -->
        <collection property="c_list" ofType="com.sist.app.vo.CommVO" select="com.sist.app.mapper.CommMapper.commList" column="b_idx"/> 
    </resultMap>

    <!--총 게시물의 수를 반환-->
    <select id="count" resultType="int" parameterType="Map">
        SELECT COUNT(*) FROM bbs_t
        <where>
            bname=#{bname} AND status = 0
            <if test="searchType != null and searchValue != null">
                <choose>
                    <when test="searchType == 0">
                        AND subject LIKE CONCAT('%', #{searchValue}, '%')
                    </when>
                    <when test="searchType == 1">
                        AND writer LIKE CONCAT('%', #{searchValue}, '%')
                    </when>
                    <when test="searchType == 2">
                        AND content LIKE CONCAT('%', #{searchValue}, '%')
                    </when>
                </choose>
            </if>
        </where>
    </select>

    <!--게시물 목록 반환-->
    <select id="b_list" resultMap="map1" parameterType="Map">
        SELECT * FROM (
            SELECT @RN:=@RN+1 AS rnum, a.* FROM (
                SELECT * FROM bbs_t
                <where>
                    bname=#{bname} AND status = 0
                    <if test="searchType != null and searchValue != null">
                        <choose>
                            <when test="searchType == 0">
                                AND subject LIKE CONCAT('%', #{searchValue}, '%')
                            </when>
                            <when test="searchType == 1">
                                AND writer LIKE CONCAT('%', #{searchValue}, '%')
                            </when>
                            <when test="searchType == 2">
                                AND content LIKE CONCAT('%', #{searchValue}, '%')
                            </when>
                        </choose>
                    </if>
                </where>
                ORDER BY b_idx DESC
            ) a, (SELECT @RN:=0) b
        ) c WHERE c.rnum BETWEEN #{begin} AND #{end} 
        
    </select>

	<!-- 원글저장 -->
	<insert id="add" parameterType="com.sist.app.vo.BbsVO">
		INSERT INTO bbs_t(subject, writer, content, file_name, ori_name, write_date, ip, hit, bname, status)
		VALUES (#{subject}, #{writer}, #{content}, #{file_name}, #{ori_name}, NOW(), #{ip}, 0, #{bname}, 0)
	</insert>
    
	<!-- 원글보기 -->
	<select id="getBbs" parameterType="String" resultMap="map1">
		SELECT * FROM bbs_t
		WHERE b_idx = #{num}
	</select>

    <!-- 조회수 증가 -->
	<update id="hit" parameterType="String">
		UPDATE bbs_t
		SET hit = hit + 1
		WHERE b_idx = #{b_idx}
	</update>
	
	<!-- 원글 수정 -->
	<update id="edit" parameterType="com.sist.app.vo.BbsVO">
		UPDATE bbs_t
		<trim prefix="SET" suffixOverrides=",">
			subject = #{subject},
			content = #{content}, <!-- if문을 수행하지 않을 때 ,로 끝나므로 suffixOverrides로 ,제거 -->
			<if test="file_name != null">
				file_name = #{file_name},
				ori_name = #{ori_name}
			</if>
		</trim>
		WHERE b_idx = #{b_idx}
	</update>
	
	<!-- 원글 삭제(status를 1로 변경) -->
	<update id="del" parameterType="String">
		Update bbs_t
		SET status = 1
		WHERE b_idx = #{b_idx}
	</update>
    
</mapper>